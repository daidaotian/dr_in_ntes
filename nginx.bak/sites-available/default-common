# -*- Mode: Nginx -*-

server {
    server_name _;
    listen 80 default;

    location / {
        deny all;
    }

    location /server-status {
        stub_status on;
        allow 127.0.0.1/32;
        deny all;
    }

    location = /status.html {
        return 200 "OK";
    }
}
